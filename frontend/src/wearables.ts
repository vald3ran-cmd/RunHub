/**
 * Entry-point neutro per TypeScript.
 * A runtime Metro risolve verso wearables.native.ts (iOS/Android) o
 * wearables.web.ts (web) tramite platform extensions.
 */
export * from './wearables.native';
